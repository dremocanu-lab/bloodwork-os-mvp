# Backend Modularization — Baseline

Captured before any Phase 4 code movement. This is the safety boundary
every later step in this refactor is compared against.

## Environment

- Branch: `refactor/backend-modularization`, created from `main` at
  commit `30080fbd67e74310067e225b615f0ec10b94f511` (PRs #1 FHIR Phase 1,
  #2 Alembic, #3 FHIR Phase 3 backend hardening — all merged).
- Python 3.13.13 (matches CI's `python-version: "3.13"`).
- Key backend dependency versions: see `backend/requirements.txt` /
  `backend/requirements-dev.txt` (unchanged by this phase — FastAPI
  0.115.12, SQLAlchemy 2.0.40, Alembic 1.18.4, pytest 8.3.5).

## Architecture size

| Metric | Value |
|---|---|
| `app/main.py` LOC | 8,141 |
| `app/models.py` LOC | 891 |
| Route decorators in `main.py` (`@app.get/post/patch/put/delete`) | 113 |
| Total FastAPI routes (live app, incl. framework-added `/docs`/`/openapi.json`/etc.) | 117 |
| Backend `.py` modules total (`find app -name "*.py"`) | 62 |
| Test files (`tests/test_*.py`) | 33 |
| Tests collected | 310 |

## Baseline test/quality results

All run fresh, immediately before any Phase 4 change:

- `pytest -q` (full suite, real DB): **310 passed**, 0 failed, 778s.
- `bandit -r app scripts -ll -q`: **clean** (same invocation as CI).
- `python scripts/check_migration_drift.py`: **clean** (8 known/
  tolerated legacy-index differences, explicitly named — see
  `docs/database/MIGRATIONS.md`).
- Frontend `npx tsc --noEmit`: **clean**.
- Frontend `node scripts/check-lint-baseline.mjs`: **clean** (0 new
  errors beyond the frozen 30-error baseline).
- Frontend `npm run build`: not re-run at this exact baseline snapshot
  (verified clean immediately prior, as part of PR #3's closure — no
  frontend file has changed since).

**This refactor starts from a fully green baseline.** Every step below
must return to this same green state before proceeding to the next.

## API contract snapshot

- `backend/tests/contracts/openapi_pre_modularization.json` — the full
  OpenAPI document from the live app (`app.openapi()`), 99 unique paths.
- `backend/tests/contracts/route_inventory_pre_modularization.json` —
  a flatter, more diff-friendly inventory: one entry per
  (path, methods) with route name, tags, and the actual auth dependency
  callables wired to it (read from FastAPI's own dependant tree, not
  re-derived) — 117 entries, generated by
  `backend/tests/contracts/generate_route_inventory.py`.

Both were generated directly from the running application, not
hand-transcribed. Re-run `generate_route_inventory.py` (writing to a
different path) at any later checkpoint and diff against these files —
see `docs/refactor/OPENAPI_EQUIVALENCE_REPORT.md` (produced at the end of
this phase) for the final comparison.

## Route line-number map (main.py, pre-refactor)

For reference — the exact line each route decorator starts at in
`main.py` before any code moved, used to build
`BACKEND_DECOMPOSITION_PLAN.md`'s domain boundaries. Not reproduced here
in full (117 lines); see git history of this file's own branch for the
raw extraction if needed, or re-derive with:

```
grep -n '^@app\.\(get\|post\|patch\|put\|delete\)(' app/main.py
```
