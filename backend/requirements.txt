fastapi==0.115.12
uvicorn[standard]==0.34.2
python-dotenv==1.2.2
sqlalchemy==2.0.40
psycopg[binary]==3.2.6
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.32
pydantic[email]==2.11.4
email-validator==2.2.0
openai==2.8.1
google-cloud-documentai==3.2.0
google-auth==2.36.0
PyMuPDF==1.24.14
requests==2.33.1
# Optional distributed rate-limit backend — only imported/connected when
# RATE_LIMIT_REDIS_URL (or REDIS_URL) is set; see app/rate_limit.py and
# docs/security/RATE_LIMITING.md. Installed unconditionally so activating
# it later is an env-var change, not a deploy that adds a dependency.
redis==5.2.1
# Optional real malware-scanner client — only imported/connected when
# CLAMAV_HOST is set; see app/services/security_scan.py and
# docs/security/MALWARE_SCANNING_PLAN.md. Same "installed unconditionally,
# activated by env var" pattern as redis above.
clamd==1.0.2
# Was already an implicit transitive dependency of python-jose[cryptography]
# above; pinned directly now that app/services/interop/crypto.py and jwks.py
# depend on it explicitly (secret-at-rest encryption, RSA keypair generation
# for SMART Backend Services — see BRAGI_INTEROP_PLAN.md).
cryptography==47.0.0
# Schema migrations — see docs/database/MIGRATIONS.md. A real production
# dependency (not dev-only): `scripts/run_migrations.py` runs
# `alembic upgrade head` as part of deployment, same as any other backend
# code path.
alembic==1.18.4
# Was already an implicit transitive dependency; pinned directly now that
# app/services/clinical_document/medication_duration.py depends on it
# explicitly for real calendar-month arithmetic (relativedelta) — Clinical
# Document Intelligence V3, Phase 7. Never hand-roll month-end correction.
python-dateutil==2.9.0.post0
# Universal document ingestion (BRAGI — UNIVERSAL DOCUMENT INGESTION +
# ASK BRAGI STABLE LAYOUT) — see app/services/ingestion/. Real, local text
# extraction for formats that were previously either silently unsupported
# (DOCX) or never even attempted (RTF/XLSX): avoids paying for/depending on
# a remote OCR provider for formats that already contain machine-readable
# text, and fixes the root cause of the production DOCX-classified-as-
# "other" bug (Google Document AI was never able to read it).
python-docx==1.2.0
striprtf==0.0.33
openpyxl==3.1.5
# Were already implicit transitive dependencies (Pillow via
# google-cloud-documentai's own deps; defusedxml likely via another
# package) — pinned directly now that app/services/ingestion depends on
# them explicitly: Pillow for EXIF-rotation-aware image normalization
# before OCR, defusedxml for XXE-safe parsing of uploaded XML (CDA/generic
# health-export XML) and Office-XML-based formats (DOCX/XLSX/ODT/ODS
# internally use zipped XML too, but python-docx/openpyxl parse those
# themselves — defusedxml here covers the ingestion module's OWN direct
# XML parsing, i.e. RTF is not XML but ODT/ODS/CDA/generic-XML are).
Pillow==11.2.1
defusedxml==0.7.1
# HEIC/HEIF (iPhone photo) support for the image adapter — Pillow itself
# has no built-in decoder for these; this registers one via
# pillow_heif.register_heif_opener().
pillow-heif==1.7.0